#!/usr/bin/env python
from distutils.core import setup

setup(name='Transparent Objects',
      version='1.0.0',
      description='A pipeline for transparent objects for object recognition',
      packages=['transparent_objects'],
      package_dir={'':'python'}
)
